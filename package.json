{
  "name": "fasting-tracker-pwa",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx serve .",
    "build": "mkdir -p dist && cp index.html dist/ && cp -r public/* dist/",
    "preview": "npx serve dist"
  }
}
